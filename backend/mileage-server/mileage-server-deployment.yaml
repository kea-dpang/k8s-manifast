apiVersion: apps/v1
kind: Deployment
metadata:
  name: mileage-server-deployment
  namespace: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mileage-server
  template:
    metadata:
      labels:
        app: mileage-server
    spec:
      containers:
      - name: mileage-server-container
        image: dpang-shop-dev.kr-gov-central-1.kcr.dev/dpang-backend/mileage-server:0.8.0
        ports:
        - containerPort: 8080
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nodepool
                operator: In
                values:
                - backend
