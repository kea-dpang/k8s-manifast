apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-server-deployment
  namespace: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-server
  template:
    metadata:
      labels:
        app: auth-server
    spec:
      containers:
      - name: auth-server-container
        image: dpang-shop-dev.kr-gov-central-1.kcr.dev/dpang-backend/auth-server:0.4.0
        ports:
        - containerPort: 8080
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nodepool
                operator: In
                values:
                - backend
