apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-server-deployment
  namespace: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-server
  template:
    metadata:
      labels:
        app: notification-server
    spec:
      containers:
      - name: notification-server-container
        image: dpang-shop-dev.kr-gov-central-1.kcr.dev/dpang-backend/notification-server
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - host-172-16-18-75
                - host-172-16-19-178
